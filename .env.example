# ===========================================
# Agent Architecture - Environment Configuration
# ===========================================
# Copy this file to .env and fill in your actual values

# -------------------------------------------
# Azure Cosmos DB (Database)
# -------------------------------------------
COSMOS_ENDPOINT=https://your-account.documents.azure.com:443/
COSMOS_KEY=your-cosmos-primary-key
COSMOS_DATABASE_NAME=agent-architecture-db

# -------------------------------------------
# Azure Blob Storage (File uploads)
# -------------------------------------------
AZURE_STORAGE_ACCOUNT=yourstorageaccount
AZURE_STORAGE_KEY=your-storage-account-key
AZURE_STORAGE_CONTAINER=resources

# Alternative: Use connection string instead
# AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=...

# -------------------------------------------
# Azure AI Foundry / OpenAI (AI features)
# -------------------------------------------
AZURE_AI_ENDPOINT=https://your-ai.openai.azure.com/
AZURE_AI_API_KEY=your-ai-api-key

# OpenAI-specific (if using direct OpenAI)
AZURE_OPENAI_ENDPOINT=https://your-openai.openai.azure.com/
AZURE_OPENAI_API_KEY=your-openai-key
AZURE_OPENAI_DEPLOYMENT=gpt-4o
AZURE_OPENAI_EMBEDDINGS_DEPLOYMENT=text-embedding-ada-002
AZURE_OPENAI_API_VERSION=2024-08-01-preview

# -------------------------------------------
# Azure AI Search (Optional - for transcript search)
# -------------------------------------------
AZURE_SEARCH_ENDPOINT=https://your-search.search.windows.net
AZURE_SEARCH_API_KEY=your-search-admin-key
AZURE_SEARCH_INDEX_NAME=transcripts

# -------------------------------------------
# Application Configuration
# -------------------------------------------
ENVIRONMENT=development

# CORS Origins (Update if NOT using Docker reverse proxy)
# With Docker: Use http://localhost:8080 (reverse proxy port)
# Without Docker: Use http://localhost:3008 (frontend dev port)
CORS_ORIGINS=http://localhost:8080

# API Authentication Key (Change in production!)
API_KEY=dev-test-key-123

# -------------------------------------------
# Setup Instructions:
# -------------------------------------------
# 1. Copy this file: cp .env.example .env
# 2. Update all "your-*" values with actual Azure credentials
# 3. Run: docker-compose up --build
# 4. Open: http://localhost:8080
